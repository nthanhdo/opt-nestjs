export const MOCK_HEADER = 'x-use-mock';

/**
 * ENV flag to enable mock system globally
 * FEATURE_MOCK=true
 */
export const MOCK_ENABLED_ENV = 'FEATURE_MOCK';

/**
 * Metadata key for @Mockable decorator
 */
export const MOCK_KEY = 'MOCK_KEY';
